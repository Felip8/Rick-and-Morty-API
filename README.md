# API DO RICK AND MORTY 🧪🧬🛸

Nesse código, ao invés de seguir o padrão que eu estava usando antes (usando os dados da API e dando um innerText para printar na tela), decidi colocar as informações dentro de uma tabela do HTML estilizando com CSS posteriormente.

Gostei do resultado.
E eu não sabia que tinha uma API do Rick and Morty
